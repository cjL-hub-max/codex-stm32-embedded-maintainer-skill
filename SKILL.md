---
name: stm32-freertos-simple-maintainer
description: Use when inspecting, refactoring, extending, debugging, or handing off STM32CubeMX/STM32CubeIDE FreeRTOS C projects that must remain simple, readable, main.c-like, and easy for embedded engineers to continue. Preserve portable peripheral/function modules, verify hardware from the repository before editing, avoid speculative hardware changes, keep FreeRTOS task shells thin, simplify business code, use dense useful comments and one unified serial log, run available builds/tests, and finish the work autonomously in one pass with planning, implementation results, acceptance checks, and a precise change report.
---

# STM32 FreeRTOS 简洁维护技能

## 目标

把 STM32CubeMX、STM32CubeIDE 与 FreeRTOS 工程维护成接近传统 `main.c + 一组 .c/.h` 的阅读方式。

FreeRTOS 只负责调度。主业务流程必须集中、顺序清楚、方便移交。不要为了“架构专业”增加多余层级、任务、宏、变量或短函数。

## 一次完成

收到工程和需求后，连续完成：

1. 读取工程、交接资料和当前版本。
2. 核对硬件、协议、任务、队列、中断与 DMA。
3. 识别受保护模块和可编辑业务模块。
4. 制定项目规划、实施计划与验收标准。
5. 完成最小且完整的修改。
6. 执行可用构建、测试或静态检查。
7. 一次性交付修改结果、验收结果和测试步骤。

不要为普通实现选择反复询问用户。只有未知信息无法从工程、`.ioc`、原理图、文档、历史代码或测试记录中确认，并且错误选择会影响硬件安全、协议正确性或核心功能时，才集中提出一次阻塞问题。不得猜测。

## 第一原则

- 简练易懂优先于复杂分层。
- 可编译优先于形式漂亮。
- 当前工程事实优先于经验和记忆。
- 最小改动优先于全面重构。
- 目标文件现有布局优先于统一模板。
- 可移植模块稳定接口优先于业务命名统一。
- 不能确认且影响正确性时询问，绝不猜硬件。

## 文件分类

### 可编辑业务层

通常包括：

- `freertos.c`
- 当前项目主业务 `.c/.h`
- `app_xxx.c/.h`
- 项目专用任务、模式、状态、日志和适配代码

这些文件中的变量、函数、类型、枚举和宏可以整理、删除、合并或重命名，但必须同步更新全部引用并保持可编译。

### 受保护的可移植模块

凡是能够复制到其他工程使用的外设、BSP、协议、CRC、算法、滤波器、UI 协议或通用功能 `.c/.h`，默认完整保护。

除非用户明确授权，否则：

- 不改文件名和内部布局。
- 不改公开函数、类型、枚举、宏及参数语义。
- 不改结构体布局或协议格式。
- 不加入当前项目专用模式、业务和日志。
- 不为了统一命名而修改。

业务层通过原公开接口调用这些模块。发现疑似缺陷时先报告，不擅自重构。

### CubeMX 与硬件配置文件

`.ioc`、`main.c`、GPIO、UART、TIM、DMA、I2C、IRQ 与 MSP 文件属于特殊维护区。只有需求确实涉及对应硬件且配置已有工程依据时才修改。保留 CubeMX 生成结构与 `USER CODE` 区域，不整体格式化。

## FreeRTOS 架构

采用“一个主业务任务 + 少量真正必要的独立任务”。

```text
main.c
  -> 初始化硬件并启动 FreeRTOS
freertos.c
  -> 创建任务、队列和互斥锁
  -> 保存简洁任务壳
main_task
  -> 调用现有主业务模块
主业务模块
  -> 像传统 main.c 的 while(1) 一样组织流程
  -> 调用受保护外设和算法模块
必要独立任务
  -> 阻塞等待、固定周期、耗时处理或特殊优先级功能
```

不要机械地让每个外设对应一个任务。仅当功能需要阻塞等待、独立固定周期、耗时处理、特殊优先级或完整独立运行流程时才建立任务。

若现有 `app_fusion_control.c` 等文件已经承担主业务职责，优先整理和复用，不为名称再次创建 `app_main.c`。

### `freertos.c` 规则

`freertos.c` 只负责 RTOS 资源、任务创建与简洁任务壳：

```c
void main_task(void *argument)
{
    (void)argument;

    main_init();

    while (1)
    {
        main_step();
        osDelay(10U);
    }
}
```

要求：

- 任务通常使用 `while (1)`。
- 循环内必须阻塞或调用 `osDelay` / `vTaskDelay`。
- FreeRTOS 任务中禁止使用 `HAL_Delay`。
- 复杂状态机、协议解析和业务计算不得堆进任务壳。
- 不混用多种任务创建风格，除非现有工程有明确原因。

## 保留 `.c/.h` 内部布局

修改前先识别目标文件已有顺序，例如文件头、`include`、宏、全局对象、静态状态、私有声明、编号功能区块、对外接口和私有实现。

新增代码必须进入已有对应区块。不要全文件重排、整体格式化、改全部注释风格或为小改动制造大面积差异。驱动类文件内部布局不做整理。

## 命名与代码规模

### 变量

可编辑业务变量必须：

- 使用简单英文小写。
- 最多一个下划线。
- 下划线前后各为一个简单单词。
- 名称直接说明用途。
- 必要时使用简单单位后缀。

推荐：`track_mode`、`motor_state`、`target_deg`、`delay_ms`、`frame_count`。

禁止：`current_selected_radar_target_angle_deg`、`fusionControlState`、`flag1`、`temp_value_data`。

已有业务变量允许重命名；受保护模块中的名称不改。

### 宏

慎用宏。只出现一次且含义直观的普通数值直接写入代码，例如 `osDelay(1000U)`。

仅对硬件固定参数、协议参数、标定参数、安全限位、数组大小、多处共享值和编译期配置使用命名常量或宏。新增宏必须写明用途与来源。不要使用一堆互相组合的功能开关控制运行模式。

### 模式切换

停止、纯雷达、视觉热成像和融合追踪只使用一个数字变量：

```c
static uint8_t track_mode = 1U; /* 0 停止，1 纯雷达，2 视觉与热成像，3 融合追踪。 */
```

在一个清楚的 `switch` 中集中展示主要业务支路。禁止同时保留 `radar_enable`、`camera_enable`、`thermal_enable`、`fusion_enable` 等冲突开关。

### 函数划分

- 普通业务函数约 50 到 120 行，约 100 行通常合适。
- 按完整功能划分，不按行数机械拆分。
- 算法、协议状态机、滤波、S 曲线和复杂解析不受该长度限制。
- 不允许普通函数达到数百行并混合多个职责。
- 不创建一排三五行、只包装一次调用的“小萝卜头函数”。
- 重复使用、可独立测试、输入输出明确或属于完整复杂步骤时才拆函数。

## 注释规则

注释目标是：只看注释也能基本理解程序流程。

必须为所有变量、宏、结构体成员、枚举值和函数写用途注释。每个函数必须有函数头注释，说明功能、输入、输出或返回值；必要时说明阻塞、状态修改、队列、单位、方向和调用限制。

函数内部按功能点密集注释，解释“这一步做什么、为什么做、数据流向哪里”，不要机械复述语法。

推荐：

```c
count++; /* 累计本次通信恢复失败次数。 */
```

禁止：

```c
count++; /* count 加一。 */
```

## 数据流与日志

优先采用：

- 普通同步处理：函数调用和参数传递。
- 异步命令：队列。
- 高频传感器：最新快照。
- 简单事件：已有状态、任务通知或事件。

每类数据只有一个主要写入者。主业务读取最新有效数据，不积累过期控制命令。不让受保护驱动知道当前追踪模式或整机业务。

优先复用工程已有统一日志模块。没有时才建立一个简单日志入口。所有任务日志使用同一个串口并由互斥锁保护。

日志只覆盖：

- 系统启动和模式变化。
- 大约每 1000 ms 一行主要整机状态。
- 目标出现或丢失。
- 真正下发的电机命令。
- 关键错误与恢复。

不要为不同模块创建一批日志函数，不打印每个数据处理节点，不使用大量日志宏，不按任务周期刷屏，也不修改受保护模块来加入项目专用日志。

## 硬件确认

修改前必须从当前工程核对相关硬件，至少检查：

- MCU 型号与工程目标。
- `.ioc`。
- GPIO 端口、引脚、模式和有效电平。
- UART 实例、引脚、波特率、DMA 和接收方式。
- TIM 实例、通道、频率与 DMA 请求。
- DMA 控制器、Stream、Request、方向和绑定。
- NVIC 中断入口与优先级。
- I2C、SPI、QSPI 配置与设备地址。
- 电机 STEP、DIR、EN、方向、脉冲数和机械限位。
- 摄像头尺寸、格式、内参与协议。
- 雷达串口、帧格式、单位和安装方向。
- 双板通信版本、CRC 与时间戳语义。
- 调试串口以及是否与二进制数据共用。

依据优先级：当前工程代码和 `.ioc`，其次是原理图与交接资料，再其次是当前版本测试记录和芯片手册。变量名、文件名和过去项目经验不能单独作为硬件依据。

来源冲突时明确列出，不能静默选择。

## 修改工作流

1. **确认基线**：记录仓库、分支、提交、ZIP、版本和 CubeIDE 内部工程名，禁止混用版本。
2. **阅读项目**：检查 README、架构、交接、`.ioc`、`freertos.c`、主业务、保护接口、中断、DMA、队列和任务关系。
3. **项目规划**：明确最终目标、当前状态、主要问题、目标架构、阶段与风险；规划后继续执行，不等待普通批准。
4. **实施计划**：列出准备修改和明确不修改的文件、阶段目标、验证方式和最终验收标准。
5. **最小修改**：先复用接口与状态，再整理业务、删除重复层、增加少量必要代码；只有明确独立职责时才新建文件。
6. **构建检查**：能构建时必须实际构建；不能构建时完成静态检查并如实说明。
7. **电机专项**：涉及电机任务、队列、运动、限位、方向、TIM、DMA 或中断时，检查当前工程完整定时器、DMA、IRQ、完成回调、软件位置和安全停止链路。
8. **验收**：读取并执行 [references/acceptance-checklist.md](references/acceptance-checklist.md)。
9. **交付**：按照 [references/final-report-template.md](references/final-report-template.md) 一次性报告。

## 禁止行为

- 未读工程就直接写代码。
- 猜测引脚、方向、协议、单位、DMA 或中断。
- 把一次需求拆成多轮要求用户反复批准。
- 为简单需求创建大量文件、宏、状态和函数。
- 把业务逻辑塞入受保护驱动或 `freertos.c`。
- 为统一风格重排整个文件。
- 修改保护接口后不说明。
- 未构建却声称构建通过。
- 未实机测试却声称功能完全正常。
- 只回复“修改完成”而不列出具体变化。

## 完成标准

- 主流程更容易从一个业务文件顺序阅读。
- FreeRTOS 任务壳简洁。
- 运行模式集中且只有一个主要变量。
- 变量、函数、宏和注释符合本技能规则。
- 保护模块保持不变，或存在用户明确授权的例外。
- 相关硬件配置均有当前工程依据。
- 可用构建和测试已经执行。
- 项目规划、实施结果、验收结果与测试步骤一次性交付。
